# ── DeliverIQ Backend — Development & Testing Dependencies ───────────────────
#
# Install: pip install -r requirements-dev.txt
# (Assumes requirements.txt is already installed)
#
# These packages are NOT needed in production and should NOT be in Dockerfile.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0             # Test runner — parametrize, fixtures, plugins
pytest-asyncio>=0.23.0   # Async test support (for async route handlers)
httpx>=0.27.0             # HTTP client used by FastAPI's TestClient
pytest-cov>=5.0.0        # Coverage reporting — run with: pytest --cov=app

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff>=0.4.0              # Extremely fast Python linter (replaces flake8, isort)
mypy>=1.10.0             # Static type checker — validates type hints
